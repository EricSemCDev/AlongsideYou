using UnityEngine;

// Marca um local de encaixe de operador na Estação de Cálculo (issue futura).
// Usado apenas para o PuzzleConfig contar quantos slots de operador
// a equação da fase precisa.
public class OperatorSlotMarker : MonoBehaviour
{
}